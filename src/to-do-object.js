const ToDo = (title, description, dueDate, priority, notes, checkList = []) => {
  return {
    id,
    startDate,
    title,
    description,
    dueDate,
    priority,
    notes,
    checkList,
  };
};

export default ToDo;
